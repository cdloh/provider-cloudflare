---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: rules.teams.cloudflare.upbound.io
spec:
  group: teams.cloudflare.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: Rule
    listKind: RuleList
    plural: rules
    singular: rule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Rule is the Schema for the Rules API. Provides a Cloudflare Teams
          rule resource.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RuleSpec defines the desired state of Rule
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountId:
                    description: The account to which the teams rule should be added.
                      The account identifier to target for the resource.
                    type: string
                  accountIdRef:
                    description: Reference to a Account in account to populate accountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accountIdSelector:
                    description: Selector for a Account in account to populate accountId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  action:
                    description: 'The action executed by matched teams rule. The action
                      executed by matched teams rule. Available values: `allow`, `block`,
                      `safesearch`, `ytrestricted`, `on`, `off`, `scan`, `noscan`,
                      `isolate`, `noisolate`, `override`, `l4_override`, `egress`,
                      `audit_ssh`.'
                    type: string
                  description:
                    description: The description of the teams rule. The description
                      of the teams rule.
                    type: string
                  devicePosture:
                    description: The wirefilter expression to be used for device_posture
                      check matching. The wirefilter expression to be used for device_posture
                      check matching.
                    type: string
                  enabled:
                    description: Indicator of rule enablement. Indicator of rule enablement.
                    type: boolean
                  filters:
                    description: The protocol or layer to evaluate the traffic and
                      identity expressions. The protocol or layer to evaluate the
                      traffic and identity expressions.
                    items:
                      type: string
                    type: array
                  identity:
                    description: The wirefilter expression to be used for identity
                      matching. The wirefilter expression to be used for identity
                      matching.
                    type: string
                  name:
                    description: The name of the teams rule. The name of the teams
                      rule.
                    type: string
                  precedence:
                    description: The evaluation precedence of the teams rule. The
                      evaluation precedence of the teams rule.
                    type: number
                  ruleSettings:
                    description: Additional rule settings (refer to the nested schema).
                      Additional rule settings.
                    items:
                      properties:
                        addHeaders:
                          additionalProperties:
                            type: string
                          description: Add custom headers to allowed requests in the
                            form of key-value pairs. Add custom headers to allowed
                            requests in the form of key-value pairs.
                          type: object
                        allowChildBypass:
                          description: Allow parent MSP accounts to enable bypass
                            their children's rules.
                          type: boolean
                        auditSsh:
                          description: Settings for auditing SSH usage.
                          items:
                            properties:
                              commandLogging:
                                description: Log all SSH commands.
                                type: boolean
                            type: object
                          type: array
                        bisoAdminControls:
                          description: Configure how browser isolation behaves (refer
                            to the nested schema). Configure how browser isolation
                            behaves.
                          items:
                            properties:
                              disableCopyPaste:
                                description: (Boolean) Disable copy-paste. Disable
                                  copy-paste.
                                type: boolean
                              disableDownload:
                                description: (Boolean) Disable download. Disable download.
                                type: boolean
                              disableKeyboard:
                                description: (Boolean) Disable keyboard usage. Disable
                                  keyboard usage.
                                type: boolean
                              disablePrinting:
                                description: (Boolean) Disable printing. Disable printing.
                                type: boolean
                              disableUpload:
                                description: (Boolean) Disable upload. Disable upload.
                                type: boolean
                            type: object
                          type: array
                        blockPageEnabled:
                          description: Indicator of block page enablement. Indicator
                            of block page enablement.
                          type: boolean
                        blockPageReason:
                          description: The displayed reason for a user being blocked.
                            The displayed reason for a user being blocked.
                          type: string
                        bypassParentRule:
                          description: Allow child MSP accounts to bypass their parent's
                            rule.
                          type: boolean
                        checkSession:
                          description: Configure how session check behaves (refer
                            to the nested schema). Configure how session check behaves.
                          items:
                            properties:
                              duration:
                                description: Configure how fresh the session needs
                                  to be to be considered valid. Configure how fresh
                                  the session needs to be to be considered valid.
                                type: string
                              enforce:
                                description: Enable session enforcement for this rule.
                                  Enable session enforcement for this rule.
                                type: boolean
                            type: object
                          type: array
                        egress:
                          description: Configure how Proxy traffic egresses. Can be
                            set for rules with Egress action and Egress filter. Can
                            be omitted to indicate local egress via Warp IPs.
                          items:
                            properties:
                              ipv4:
                                description: The IPv4 address to be used for egress.
                                type: string
                              ipv4Fallback:
                                description: The IPv4 address to be used for egress
                                  in the event of an error egressing with the primary
                                  IPv4. Can be '0.0.0.0' to indicate local egreass
                                  via Warp IPs.
                                type: string
                              ipv6:
                                description: The IPv6 range to be used for egress.
                                type: string
                            type: object
                          type: array
                        insecureDisableDnssecValidation:
                          description: Disable DNSSEC validation (must be Allow rule)
                            Disable DNSSEC validation (must be Allow rule).
                          type: boolean
                        ipCategories:
                          description: Turns on IP category based filter on dns if
                            the rule contains dns category checks.
                          type: boolean
                        l4override:
                          description: Settings to forward layer 4 traffic (refer
                            to the nested schema). Settings to forward layer 4 traffic.
                          items:
                            properties:
                              ip:
                                description: Override IP to forward traffic to. Override
                                  IP to forward traffic to.
                                type: string
                              port:
                                description: Override Port to forward traffic to.
                                  Override Port to forward traffic to.
                                type: number
                            type: object
                          type: array
                        overrideHost:
                          description: The host to override matching DNS queries with.
                            The host to override matching DNS queries with.
                          type: string
                        overrideIps:
                          description: The IPs to override matching DNS queries with.
                            The IPs to override matching DNS queries with.
                          items:
                            type: string
                          type: array
                        payloadLog:
                          description: Configure DLP Payload Logging settings for
                            this rule.
                          items:
                            properties:
                              enabled:
                                description: Indicator of rule enablement. Enable
                                  or disable DLP Payload Logging for this rule.
                                type: boolean
                            type: object
                          type: array
                        untrustedCert:
                          description: Configure untrusted certificate settings for
                            this rule.
                          items:
                            properties:
                              action:
                                description: 'The action executed by matched teams
                                  rule. Action to be taken when the SSL certificate
                                  of upstream is invalid. Available values: `pass_through`,
                                  `block`, `error`.'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  traffic:
                    description: The wirefilter expression to be used for traffic
                      matching. The wirefilter expression to be used for traffic matching.
                    type: string
                type: object
              initProvider:
                description: THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. InitProvider
                  holds the same fields as ForProvider, with the exception of Identifier
                  and other resource reference fields. The fields that are in InitProvider
                  are merged into ForProvider when the resource is created. The same
                  fields are also added to the terraform ignore_changes hook, to avoid
                  updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after
                  creation, for example because of an external controller is managing
                  them, like an autoscaler.
                properties:
                  action:
                    description: 'The action executed by matched teams rule. The action
                      executed by matched teams rule. Available values: `allow`, `block`,
                      `safesearch`, `ytrestricted`, `on`, `off`, `scan`, `noscan`,
                      `isolate`, `noisolate`, `override`, `l4_override`, `egress`,
                      `audit_ssh`.'
                    type: string
                  description:
                    description: The description of the teams rule. The description
                      of the teams rule.
                    type: string
                  devicePosture:
                    description: The wirefilter expression to be used for device_posture
                      check matching. The wirefilter expression to be used for device_posture
                      check matching.
                    type: string
                  enabled:
                    description: Indicator of rule enablement. Indicator of rule enablement.
                    type: boolean
                  filters:
                    description: The protocol or layer to evaluate the traffic and
                      identity expressions. The protocol or layer to evaluate the
                      traffic and identity expressions.
                    items:
                      type: string
                    type: array
                  identity:
                    description: The wirefilter expression to be used for identity
                      matching. The wirefilter expression to be used for identity
                      matching.
                    type: string
                  name:
                    description: The name of the teams rule. The name of the teams
                      rule.
                    type: string
                  precedence:
                    description: The evaluation precedence of the teams rule. The
                      evaluation precedence of the teams rule.
                    type: number
                  ruleSettings:
                    description: Additional rule settings (refer to the nested schema).
                      Additional rule settings.
                    items:
                      properties:
                        addHeaders:
                          additionalProperties:
                            type: string
                          description: Add custom headers to allowed requests in the
                            form of key-value pairs. Add custom headers to allowed
                            requests in the form of key-value pairs.
                          type: object
                        allowChildBypass:
                          description: Allow parent MSP accounts to enable bypass
                            their children's rules.
                          type: boolean
                        auditSsh:
                          description: Settings for auditing SSH usage.
                          items:
                            properties:
                              commandLogging:
                                description: Log all SSH commands.
                                type: boolean
                            type: object
                          type: array
                        bisoAdminControls:
                          description: Configure how browser isolation behaves (refer
                            to the nested schema). Configure how browser isolation
                            behaves.
                          items:
                            properties:
                              disableCopyPaste:
                                description: (Boolean) Disable copy-paste. Disable
                                  copy-paste.
                                type: boolean
                              disableDownload:
                                description: (Boolean) Disable download. Disable download.
                                type: boolean
                              disableKeyboard:
                                description: (Boolean) Disable keyboard usage. Disable
                                  keyboard usage.
                                type: boolean
                              disablePrinting:
                                description: (Boolean) Disable printing. Disable printing.
                                type: boolean
                              disableUpload:
                                description: (Boolean) Disable upload. Disable upload.
                                type: boolean
                            type: object
                          type: array
                        blockPageEnabled:
                          description: Indicator of block page enablement. Indicator
                            of block page enablement.
                          type: boolean
                        blockPageReason:
                          description: The displayed reason for a user being blocked.
                            The displayed reason for a user being blocked.
                          type: string
                        bypassParentRule:
                          description: Allow child MSP accounts to bypass their parent's
                            rule.
                          type: boolean
                        checkSession:
                          description: Configure how session check behaves (refer
                            to the nested schema). Configure how session check behaves.
                          items:
                            properties:
                              duration:
                                description: Configure how fresh the session needs
                                  to be to be considered valid. Configure how fresh
                                  the session needs to be to be considered valid.
                                type: string
                              enforce:
                                description: Enable session enforcement for this rule.
                                  Enable session enforcement for this rule.
                                type: boolean
                            type: object
                          type: array
                        egress:
                          description: Configure how Proxy traffic egresses. Can be
                            set for rules with Egress action and Egress filter. Can
                            be omitted to indicate local egress via Warp IPs.
                          items:
                            properties:
                              ipv4:
                                description: The IPv4 address to be used for egress.
                                type: string
                              ipv4Fallback:
                                description: The IPv4 address to be used for egress
                                  in the event of an error egressing with the primary
                                  IPv4. Can be '0.0.0.0' to indicate local egreass
                                  via Warp IPs.
                                type: string
                              ipv6:
                                description: The IPv6 range to be used for egress.
                                type: string
                            type: object
                          type: array
                        insecureDisableDnssecValidation:
                          description: Disable DNSSEC validation (must be Allow rule)
                            Disable DNSSEC validation (must be Allow rule).
                          type: boolean
                        ipCategories:
                          description: Turns on IP category based filter on dns if
                            the rule contains dns category checks.
                          type: boolean
                        l4override:
                          description: Settings to forward layer 4 traffic (refer
                            to the nested schema). Settings to forward layer 4 traffic.
                          items:
                            properties:
                              ip:
                                description: Override IP to forward traffic to. Override
                                  IP to forward traffic to.
                                type: string
                              port:
                                description: Override Port to forward traffic to.
                                  Override Port to forward traffic to.
                                type: number
                            type: object
                          type: array
                        overrideHost:
                          description: The host to override matching DNS queries with.
                            The host to override matching DNS queries with.
                          type: string
                        overrideIps:
                          description: The IPs to override matching DNS queries with.
                            The IPs to override matching DNS queries with.
                          items:
                            type: string
                          type: array
                        payloadLog:
                          description: Configure DLP Payload Logging settings for
                            this rule.
                          items:
                            properties:
                              enabled:
                                description: Indicator of rule enablement. Enable
                                  or disable DLP Payload Logging for this rule.
                                type: boolean
                            type: object
                          type: array
                        untrustedCert:
                          description: Configure untrusted certificate settings for
                            this rule.
                          items:
                            properties:
                              action:
                                description: 'The action executed by matched teams
                                  rule. Action to be taken when the SSL certificate
                                  of upstream is invalid. Available values: `pass_through`,
                                  `block`, `error`.'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  traffic:
                    description: The wirefilter expression to be used for traffic
                      matching. The wirefilter expression to be used for traffic matching.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: action is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.action)
                || has(self.initProvider.action)'
            - message: description is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.description)
                || has(self.initProvider.description)'
            - message: name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || has(self.initProvider.name)'
            - message: precedence is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.precedence)
                || has(self.initProvider.precedence)'
          status:
            description: RuleStatus defines the observed state of Rule.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: The account to which the teams rule should be added.
                      The account identifier to target for the resource.
                    type: string
                  action:
                    description: 'The action executed by matched teams rule. The action
                      executed by matched teams rule. Available values: `allow`, `block`,
                      `safesearch`, `ytrestricted`, `on`, `off`, `scan`, `noscan`,
                      `isolate`, `noisolate`, `override`, `l4_override`, `egress`,
                      `audit_ssh`.'
                    type: string
                  description:
                    description: The description of the teams rule. The description
                      of the teams rule.
                    type: string
                  devicePosture:
                    description: The wirefilter expression to be used for device_posture
                      check matching. The wirefilter expression to be used for device_posture
                      check matching.
                    type: string
                  enabled:
                    description: Indicator of rule enablement. Indicator of rule enablement.
                    type: boolean
                  filters:
                    description: The protocol or layer to evaluate the traffic and
                      identity expressions. The protocol or layer to evaluate the
                      traffic and identity expressions.
                    items:
                      type: string
                    type: array
                  id:
                    type: string
                  identity:
                    description: The wirefilter expression to be used for identity
                      matching. The wirefilter expression to be used for identity
                      matching.
                    type: string
                  name:
                    description: The name of the teams rule. The name of the teams
                      rule.
                    type: string
                  precedence:
                    description: The evaluation precedence of the teams rule. The
                      evaluation precedence of the teams rule.
                    type: number
                  ruleSettings:
                    description: Additional rule settings (refer to the nested schema).
                      Additional rule settings.
                    items:
                      properties:
                        addHeaders:
                          additionalProperties:
                            type: string
                          description: Add custom headers to allowed requests in the
                            form of key-value pairs. Add custom headers to allowed
                            requests in the form of key-value pairs.
                          type: object
                        allowChildBypass:
                          description: Allow parent MSP accounts to enable bypass
                            their children's rules.
                          type: boolean
                        auditSsh:
                          description: Settings for auditing SSH usage.
                          items:
                            properties:
                              commandLogging:
                                description: Log all SSH commands.
                                type: boolean
                            type: object
                          type: array
                        bisoAdminControls:
                          description: Configure how browser isolation behaves (refer
                            to the nested schema). Configure how browser isolation
                            behaves.
                          items:
                            properties:
                              disableCopyPaste:
                                description: (Boolean) Disable copy-paste. Disable
                                  copy-paste.
                                type: boolean
                              disableDownload:
                                description: (Boolean) Disable download. Disable download.
                                type: boolean
                              disableKeyboard:
                                description: (Boolean) Disable keyboard usage. Disable
                                  keyboard usage.
                                type: boolean
                              disablePrinting:
                                description: (Boolean) Disable printing. Disable printing.
                                type: boolean
                              disableUpload:
                                description: (Boolean) Disable upload. Disable upload.
                                type: boolean
                            type: object
                          type: array
                        blockPageEnabled:
                          description: Indicator of block page enablement. Indicator
                            of block page enablement.
                          type: boolean
                        blockPageReason:
                          description: The displayed reason for a user being blocked.
                            The displayed reason for a user being blocked.
                          type: string
                        bypassParentRule:
                          description: Allow child MSP accounts to bypass their parent's
                            rule.
                          type: boolean
                        checkSession:
                          description: Configure how session check behaves (refer
                            to the nested schema). Configure how session check behaves.
                          items:
                            properties:
                              duration:
                                description: Configure how fresh the session needs
                                  to be to be considered valid. Configure how fresh
                                  the session needs to be to be considered valid.
                                type: string
                              enforce:
                                description: Enable session enforcement for this rule.
                                  Enable session enforcement for this rule.
                                type: boolean
                            type: object
                          type: array
                        egress:
                          description: Configure how Proxy traffic egresses. Can be
                            set for rules with Egress action and Egress filter. Can
                            be omitted to indicate local egress via Warp IPs.
                          items:
                            properties:
                              ipv4:
                                description: The IPv4 address to be used for egress.
                                type: string
                              ipv4Fallback:
                                description: The IPv4 address to be used for egress
                                  in the event of an error egressing with the primary
                                  IPv4. Can be '0.0.0.0' to indicate local egreass
                                  via Warp IPs.
                                type: string
                              ipv6:
                                description: The IPv6 range to be used for egress.
                                type: string
                            type: object
                          type: array
                        insecureDisableDnssecValidation:
                          description: Disable DNSSEC validation (must be Allow rule)
                            Disable DNSSEC validation (must be Allow rule).
                          type: boolean
                        ipCategories:
                          description: Turns on IP category based filter on dns if
                            the rule contains dns category checks.
                          type: boolean
                        l4override:
                          description: Settings to forward layer 4 traffic (refer
                            to the nested schema). Settings to forward layer 4 traffic.
                          items:
                            properties:
                              ip:
                                description: Override IP to forward traffic to. Override
                                  IP to forward traffic to.
                                type: string
                              port:
                                description: Override Port to forward traffic to.
                                  Override Port to forward traffic to.
                                type: number
                            type: object
                          type: array
                        overrideHost:
                          description: The host to override matching DNS queries with.
                            The host to override matching DNS queries with.
                          type: string
                        overrideIps:
                          description: The IPs to override matching DNS queries with.
                            The IPs to override matching DNS queries with.
                          items:
                            type: string
                          type: array
                        payloadLog:
                          description: Configure DLP Payload Logging settings for
                            this rule.
                          items:
                            properties:
                              enabled:
                                description: Indicator of rule enablement. Enable
                                  or disable DLP Payload Logging for this rule.
                                type: boolean
                            type: object
                          type: array
                        untrustedCert:
                          description: Configure untrusted certificate settings for
                            this rule.
                          items:
                            properties:
                              action:
                                description: 'The action executed by matched teams
                                  rule. Action to be taken when the SSL certificate
                                  of upstream is invalid. Available values: `pass_through`,
                                  `block`, `error`.'
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  traffic:
                    description: The wirefilter expression to be used for traffic
                      matching. The wirefilter expression to be used for traffic matching.
                    type: string
                  version:
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
