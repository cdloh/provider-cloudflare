---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: accounts.teams.cloudflare.upbound.io
spec:
  group: teams.cloudflare.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - cloudflare
    kind: Account
    listKind: AccountList
    plural: accounts
    singular: account
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Account is the Schema for the Accounts API. Provides a Cloudflare
          Teams Account resource.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AccountSpec defines the desired state of Account
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountId:
                    description: The account to which the teams location should be
                      added. The account identifier to target for the resource.
                    type: string
                  accountIdRef:
                    description: Reference to a Account in account to populate accountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accountIdSelector:
                    description: Selector for a Account in account to populate accountId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  activityLogEnabled:
                    description: Indicator of enablement. Whether to enable the activity
                      log.
                    type: boolean
                  antivirus:
                    description: Configuration block for antivirus traffic scanning.
                      Configuration block for antivirus traffic scanning.
                    items:
                      properties:
                        enabledDownloadPhase:
                          description: Scan on file download. Scan on file download.
                          type: boolean
                        enabledUploadPhase:
                          description: Scan on file upload. Scan on file upload.
                          type: boolean
                        failClosed:
                          description: Block requests for files that cannot be scanned.
                            Block requests for files that cannot be scanned.
                          type: boolean
                      type: object
                    type: array
                  blockPage:
                    description: Configuration for a custom block page. Configuration
                      for a custom block page.
                    items:
                      properties:
                        backgroundColor:
                          description: Hex code of block page background color. Hex
                            code of block page background color.
                          type: string
                        enabled:
                          description: Indicator of enablement. Indicator of enablement.
                          type: boolean
                        footerText:
                          description: Block page header text. Block page footer text.
                          type: string
                        headerText:
                          description: Block page footer text. Block page header text.
                          type: string
                        logoPath:
                          description: URL of block page logo. URL of block page logo.
                          type: string
                        mailtoAddress:
                          description: Admin email for users to contact.
                          type: string
                        mailtoSubject:
                          description: Subject line for emails created from block
                            page.
                          type: string
                        name:
                          description: Name of block page configuration. Name of block
                            page configuration.
                          type: string
                      type: object
                    type: array
                  bodyScanning:
                    description: Configuration for body scanning.
                    items:
                      properties:
                        inspectionMode:
                          description: 'Body scanning inspection mode. Available values:
                            `deep`, `shallow`.'
                          type: string
                      type: object
                    type: array
                  fips:
                    description: Configure compliance with Federal Information Processing
                      Standards. Configure compliance with Federal Information Processing
                      Standards.
                    items:
                      properties:
                        tls:
                          description: Only allow FIPS-compliant TLS configuration.
                            Only allow FIPS-compliant TLS configuration.
                          type: boolean
                      type: object
                    type: array
                  logging:
                    items:
                      properties:
                        redactPii:
                          description: 'Redact personally identifiable information
                            from activity logging (PII fields are: source IP, user
                            email, user ID, device ID, URL, referrer, user agent).
                            Redact personally identifiable information from activity
                            logging (PII fields are: source IP, user email, user ID,
                            device ID, URL, referrer, user agent).'
                          type: boolean
                        settingsByRuleType:
                          description: Represents whether all requests are logged
                            or only the blocked requests are logged in DNS, HTTP and
                            L4 filters. Represents whether all requests are logged
                            or only the blocked requests are slogged in DNS, HTTP
                            and L4 filters.
                          items:
                            properties:
                              dns:
                                description: Logging configuration for DNS requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                              http:
                                description: Logging configuration for HTTP requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                              l4:
                                description: Logging configuration for layer 4 requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  nonIdentityBrowserIsolationEnabled:
                    description: Indicator of enablement. Enable non-identity onramp
                      for Browser Isolation. Defaults to `false`.
                    type: boolean
                  payloadLog:
                    description: Configuration for DLP Payload Logging.
                    items:
                      properties:
                        publicKey:
                          description: Public key used to encrypt matched payloads.
                          type: string
                      type: object
                    type: array
                  protocolDetectionEnabled:
                    description: Indicator of enablement. Indicator that protocol
                      detection is enabled.
                    type: boolean
                  proxy:
                    description: Configuration block for specifying which protocols
                      are proxied. Configuration block for specifying which protocols
                      are proxied.
                    items:
                      properties:
                        rootCa:
                          description: Whether root ca is enabled account wide for
                            ZT clients.
                          type: boolean
                        tcp:
                          description: Whether gateway proxy is enabled on gateway
                            devices for tcp traffic. Whether gateway proxy is enabled
                            on gateway devices for TCP traffic.
                          type: boolean
                        udp:
                          description: Whether gateway proxy is enabled on gateway
                            devices for udp traffic. Whether gateway proxy is enabled
                            on gateway devices for UDP traffic.
                          type: boolean
                      type: object
                    type: array
                  sshSessionLog:
                    description: Configuration for SSH Session Logging.
                    items:
                      properties:
                        publicKey:
                          description: Public key used to encrypt ssh session.
                          type: string
                      type: object
                    type: array
                  tlsDecryptEnabled:
                    description: Indicator that decryption of TLS traffic is enabled.
                      Indicator that decryption of TLS traffic is enabled.
                    type: boolean
                  urlBrowserIsolationEnabled:
                    description: Safely browse websites in Browser Isolation through
                      a URL. Safely browse websites in Browser Isolation through a
                      URL. Defaults to `false`.
                    type: boolean
                type: object
              initProvider:
                description: THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. InitProvider
                  holds the same fields as ForProvider, with the exception of Identifier
                  and other resource reference fields. The fields that are in InitProvider
                  are merged into ForProvider when the resource is created. The same
                  fields are also added to the terraform ignore_changes hook, to avoid
                  updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after
                  creation, for example because of an external controller is managing
                  them, like an autoscaler.
                properties:
                  activityLogEnabled:
                    description: Indicator of enablement. Whether to enable the activity
                      log.
                    type: boolean
                  antivirus:
                    description: Configuration block for antivirus traffic scanning.
                      Configuration block for antivirus traffic scanning.
                    items:
                      properties:
                        enabledDownloadPhase:
                          description: Scan on file download. Scan on file download.
                          type: boolean
                        enabledUploadPhase:
                          description: Scan on file upload. Scan on file upload.
                          type: boolean
                        failClosed:
                          description: Block requests for files that cannot be scanned.
                            Block requests for files that cannot be scanned.
                          type: boolean
                      type: object
                    type: array
                  blockPage:
                    description: Configuration for a custom block page. Configuration
                      for a custom block page.
                    items:
                      properties:
                        backgroundColor:
                          description: Hex code of block page background color. Hex
                            code of block page background color.
                          type: string
                        enabled:
                          description: Indicator of enablement. Indicator of enablement.
                          type: boolean
                        footerText:
                          description: Block page header text. Block page footer text.
                          type: string
                        headerText:
                          description: Block page footer text. Block page header text.
                          type: string
                        logoPath:
                          description: URL of block page logo. URL of block page logo.
                          type: string
                        mailtoAddress:
                          description: Admin email for users to contact.
                          type: string
                        mailtoSubject:
                          description: Subject line for emails created from block
                            page.
                          type: string
                        name:
                          description: Name of block page configuration. Name of block
                            page configuration.
                          type: string
                      type: object
                    type: array
                  bodyScanning:
                    description: Configuration for body scanning.
                    items:
                      properties:
                        inspectionMode:
                          description: 'Body scanning inspection mode. Available values:
                            `deep`, `shallow`.'
                          type: string
                      type: object
                    type: array
                  fips:
                    description: Configure compliance with Federal Information Processing
                      Standards. Configure compliance with Federal Information Processing
                      Standards.
                    items:
                      properties:
                        tls:
                          description: Only allow FIPS-compliant TLS configuration.
                            Only allow FIPS-compliant TLS configuration.
                          type: boolean
                      type: object
                    type: array
                  logging:
                    items:
                      properties:
                        redactPii:
                          description: 'Redact personally identifiable information
                            from activity logging (PII fields are: source IP, user
                            email, user ID, device ID, URL, referrer, user agent).
                            Redact personally identifiable information from activity
                            logging (PII fields are: source IP, user email, user ID,
                            device ID, URL, referrer, user agent).'
                          type: boolean
                        settingsByRuleType:
                          description: Represents whether all requests are logged
                            or only the blocked requests are logged in DNS, HTTP and
                            L4 filters. Represents whether all requests are logged
                            or only the blocked requests are slogged in DNS, HTTP
                            and L4 filters.
                          items:
                            properties:
                              dns:
                                description: Logging configuration for DNS requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                              http:
                                description: Logging configuration for HTTP requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                              l4:
                                description: Logging configuration for layer 4 requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  nonIdentityBrowserIsolationEnabled:
                    description: Indicator of enablement. Enable non-identity onramp
                      for Browser Isolation. Defaults to `false`.
                    type: boolean
                  payloadLog:
                    description: Configuration for DLP Payload Logging.
                    items:
                      properties:
                        publicKey:
                          description: Public key used to encrypt matched payloads.
                          type: string
                      type: object
                    type: array
                  protocolDetectionEnabled:
                    description: Indicator of enablement. Indicator that protocol
                      detection is enabled.
                    type: boolean
                  proxy:
                    description: Configuration block for specifying which protocols
                      are proxied. Configuration block for specifying which protocols
                      are proxied.
                    items:
                      properties:
                        rootCa:
                          description: Whether root ca is enabled account wide for
                            ZT clients.
                          type: boolean
                        tcp:
                          description: Whether gateway proxy is enabled on gateway
                            devices for tcp traffic. Whether gateway proxy is enabled
                            on gateway devices for TCP traffic.
                          type: boolean
                        udp:
                          description: Whether gateway proxy is enabled on gateway
                            devices for udp traffic. Whether gateway proxy is enabled
                            on gateway devices for UDP traffic.
                          type: boolean
                      type: object
                    type: array
                  sshSessionLog:
                    description: Configuration for SSH Session Logging.
                    items:
                      properties:
                        publicKey:
                          description: Public key used to encrypt ssh session.
                          type: string
                      type: object
                    type: array
                  tlsDecryptEnabled:
                    description: Indicator that decryption of TLS traffic is enabled.
                      Indicator that decryption of TLS traffic is enabled.
                    type: boolean
                  urlBrowserIsolationEnabled:
                    description: Safely browse websites in Browser Isolation through
                      a URL. Safely browse websites in Browser Isolation through a
                      URL. Defaults to `false`.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: AccountStatus defines the observed state of Account.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: The account to which the teams location should be
                      added. The account identifier to target for the resource.
                    type: string
                  activityLogEnabled:
                    description: Indicator of enablement. Whether to enable the activity
                      log.
                    type: boolean
                  antivirus:
                    description: Configuration block for antivirus traffic scanning.
                      Configuration block for antivirus traffic scanning.
                    items:
                      properties:
                        enabledDownloadPhase:
                          description: Scan on file download. Scan on file download.
                          type: boolean
                        enabledUploadPhase:
                          description: Scan on file upload. Scan on file upload.
                          type: boolean
                        failClosed:
                          description: Block requests for files that cannot be scanned.
                            Block requests for files that cannot be scanned.
                          type: boolean
                      type: object
                    type: array
                  blockPage:
                    description: Configuration for a custom block page. Configuration
                      for a custom block page.
                    items:
                      properties:
                        backgroundColor:
                          description: Hex code of block page background color. Hex
                            code of block page background color.
                          type: string
                        enabled:
                          description: Indicator of enablement. Indicator of enablement.
                          type: boolean
                        footerText:
                          description: Block page header text. Block page footer text.
                          type: string
                        headerText:
                          description: Block page footer text. Block page header text.
                          type: string
                        logoPath:
                          description: URL of block page logo. URL of block page logo.
                          type: string
                        mailtoAddress:
                          description: Admin email for users to contact.
                          type: string
                        mailtoSubject:
                          description: Subject line for emails created from block
                            page.
                          type: string
                        name:
                          description: Name of block page configuration. Name of block
                            page configuration.
                          type: string
                      type: object
                    type: array
                  bodyScanning:
                    description: Configuration for body scanning.
                    items:
                      properties:
                        inspectionMode:
                          description: 'Body scanning inspection mode. Available values:
                            `deep`, `shallow`.'
                          type: string
                      type: object
                    type: array
                  fips:
                    description: Configure compliance with Federal Information Processing
                      Standards. Configure compliance with Federal Information Processing
                      Standards.
                    items:
                      properties:
                        tls:
                          description: Only allow FIPS-compliant TLS configuration.
                            Only allow FIPS-compliant TLS configuration.
                          type: boolean
                      type: object
                    type: array
                  id:
                    type: string
                  logging:
                    items:
                      properties:
                        redactPii:
                          description: 'Redact personally identifiable information
                            from activity logging (PII fields are: source IP, user
                            email, user ID, device ID, URL, referrer, user agent).
                            Redact personally identifiable information from activity
                            logging (PII fields are: source IP, user email, user ID,
                            device ID, URL, referrer, user agent).'
                          type: boolean
                        settingsByRuleType:
                          description: Represents whether all requests are logged
                            or only the blocked requests are logged in DNS, HTTP and
                            L4 filters. Represents whether all requests are logged
                            or only the blocked requests are slogged in DNS, HTTP
                            and L4 filters.
                          items:
                            properties:
                              dns:
                                description: Logging configuration for DNS requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                              http:
                                description: Logging configuration for HTTP requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                              l4:
                                description: Logging configuration for layer 4 requests.
                                items:
                                  properties:
                                    logAll:
                                      description: Whether to log all activity.
                                      type: boolean
                                    logBlocks:
                                      type: boolean
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  nonIdentityBrowserIsolationEnabled:
                    description: Indicator of enablement. Enable non-identity onramp
                      for Browser Isolation. Defaults to `false`.
                    type: boolean
                  payloadLog:
                    description: Configuration for DLP Payload Logging.
                    items:
                      properties:
                        publicKey:
                          description: Public key used to encrypt matched payloads.
                          type: string
                      type: object
                    type: array
                  protocolDetectionEnabled:
                    description: Indicator of enablement. Indicator that protocol
                      detection is enabled.
                    type: boolean
                  proxy:
                    description: Configuration block for specifying which protocols
                      are proxied. Configuration block for specifying which protocols
                      are proxied.
                    items:
                      properties:
                        rootCa:
                          description: Whether root ca is enabled account wide for
                            ZT clients.
                          type: boolean
                        tcp:
                          description: Whether gateway proxy is enabled on gateway
                            devices for tcp traffic. Whether gateway proxy is enabled
                            on gateway devices for TCP traffic.
                          type: boolean
                        udp:
                          description: Whether gateway proxy is enabled on gateway
                            devices for udp traffic. Whether gateway proxy is enabled
                            on gateway devices for UDP traffic.
                          type: boolean
                      type: object
                    type: array
                  sshSessionLog:
                    description: Configuration for SSH Session Logging.
                    items:
                      properties:
                        publicKey:
                          description: Public key used to encrypt ssh session.
                          type: string
                      type: object
                    type: array
                  tlsDecryptEnabled:
                    description: Indicator that decryption of TLS traffic is enabled.
                      Indicator that decryption of TLS traffic is enabled.
                    type: boolean
                  urlBrowserIsolationEnabled:
                    description: Safely browse websites in Browser Isolation through
                      a URL. Safely browse websites in Browser Isolation through a
                      URL. Defaults to `false`.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
